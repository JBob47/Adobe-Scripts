This script is for saving versions in Indesign and will do the following with an open Indesign Document:

- Create an archive folder inside the current active document directory
- Export a pdf of active document to Archive folder
- Save a copy of the active document to the Archive folder 
- Append the current timestamp to the end of the file name of both documents